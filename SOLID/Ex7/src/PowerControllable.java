// Split from the fat SmartClassroomDevice interface.
// Each interface represents one capability a device might have.

public interface PowerControllable {
    void powerOn();
    void powerOff();
}
